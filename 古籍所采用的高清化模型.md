# 古籍所采用的高清化模型

在对古籍（如法藏、英藏敦煌文献扫描件）进行智能化高清化和修复的过程中，本项目构建了一套由 **去网格 (Descreening)**、**Restormer（去模糊预处理）**、**ResShift（扩散超分辨率放大）** 与 **Reinhard（色彩校正后处理）** 协同组成的完整 4 步流水线（Pipeline）。

本文档详细记录了该流水线在远程服务器 `ruanxh-bbd5` 中的模型配置、权重路径及项目目录结构。

---

## 一、 核心高清化模块介绍

### 1. Frequency Descreening (频域去网格)
*   **定位**：第 1 步预处理。
*   **原理**：利用快速傅里叶变换（FFT）将图像转换到频域，自适应检测和滤除图像中由于纸张网纹、半色调印刷或扫描网点引起的高频网点噪声（使用陷波滤波器），从而消除背景网纹，为后续模型提供干净的基面。
*   **核心实现与运行脚本**：`/mnt/ruanxh/7891/scripts/frequency_descreen.py`

### 2. Restormer (图像去模糊/去噪)
*   **定位**：第 2 步预处理。
*   **原理**：基于高效 Transformer 架构（Multi-Dconv Head Transformed Attention, MDTA），在不损失高频细节的前提下，有效去除古籍图像数字化扫描过程中产生的运动模糊、对焦模糊及高斯噪声。
*   **代码仓库目录**：`/mnt/ruanxh/Restormer`
*   **核心模型权重**：
    *   **运动去模糊预训练权重 (Pretrained)**：`/mnt/ruanxh/Restormer/Motion_Deblurring/pretrained_models/motion_deblurring.pth`
    *   **古籍微调训练权重 (Fine-tuned)**：`/mnt/ruanxh/7891/experiments/models/restormer_motion.pth`
*   **单元推理脚本**：`/mnt/ruanxh/7891/scripts/restormer_deblur.py`

### 3. ResShift (扩散超分辨率放大)
*   **定位**：第 3 步核心放大。
*   **原理**：基于新型残差移位（Residual Shifting）扩散模型。相比于传统扩散模型（Diffusion Models），它大幅缩短了推理步数（只需 4~15 步采样），能够极其逼真地生成字体的边缘高频细节，对古籍文字重建效果极佳。
*   **代码仓库目录**：`/mnt/ruanxh/ResShift`
*   **核心模型权重**：
    *   **RealSR v3 真实场景超分权重 (4 步采样)**：`/mnt/ruanxh/ResShift/weights/resshift_realsrx4_s4_v3.pth`
    *   **RealSR v1 真实场景超分权重 (15 步采样)**：`/mnt/ruanxh/ResShift/weights/resshift_realsrx4_s15_v1.pth`
    *   **VQ-VAE 自动编码器权重 (核心组件)**：`/mnt/ruanxh/ResShift/weights/autoencoder_vq_f4.pth`
    *   **古籍微调训练权重**：`/mnt/ruanxh/experiments/models/resshift_x4.pth`
*   **单元推理脚本**：`/mnt/ruanxh/7891/scripts/resshift_sr.py`

### 4. Reinhard (经典色彩对齐/校正)
*   **定位**：第 4 步后处理。
*   **原理**：将超分放大后的图像转换到感知均匀的 Lab 色彩空间，计算并对齐其均值与标准差至原始低清图像。由于扩散模型（如 ResShift）在重建高频文字时易引入色偏或过饱和现象，Reinhard 算法能完美将图像色调还原为原图的古风古色。
*   **核心实现与运行脚本**：`/mnt/ruanxh/7891/scripts/reinhard_color_corr.py`

---

## 二、 高清化流程目录结构 (`/mnt/ruanxh/7891`)

```text
/mnt/ruanxh/7891/
├── scripts/                              # 核心流程脚本目录
│   ├── pipeline.py                       # 【主管线】一键式高清化管线脚本 (Descreen -> Deblur -> SR -> Color Correct)
│   ├── pipeline_ancient_book.py          # 原有古籍高清化处理管线脚本
│   ├── restormer_deblur.py               # Restormer 去模糊脚本
│   ├── resshift_sr.py                    # ResShift 超分辨率脚本
│   ├── reinhard_color_corr.py            # Reinhard 色彩校正脚本
│   ├── frequency_descreen.py             # 频域网点去除脚本 (Descreening)
│   └── __pycache__/                      # Python 编译缓存文件
│
├── experiments/                          # 实验、模型权重与结果输出目录
│   ├── models/                           
│   │   └── restormer_motion.pth          # Restormer 运动去模糊模型权重
│   │
│   ├── pipeline_results/                 # 流程化处理中间结果输出目录
│   │   ├── step1_descreen/               # 步骤 1：去网纹结果
│   │   ├── step2_deblur/                 # 步骤 2：去模糊结果
│   │   ├── step3_sr/                     # 步骤 3：超分放大结果
│   │   └── step4_color_correct/          # 步骤 4：色彩校正最终结果
│   │
│   ├── evaluation/                       # 指标评估与消融实验目录
│   │   ├── qualitative_comparison/       # 定性对比结果
│   │   └── ablation_study/               # 消融实验对比
│   │
│   └── training_logs/                    # 模型训练日志（含 TensorBoard 监控）
│
├── data/                                 # 模型训练与测试数据集目录
│   ├── train/                            # 训练集
│   ├── val/                              # 验证集
│   └── test/                             # 测试集
│
├── Real_image/                           # 真实待测试的图像目录
│   ├── 参考图像/                         # 色彩参考图
│   └── 低清图像/                         
│       └── test_book.jpg                 # 待测试的低清古籍图像
│
├── img_process/                          # 图像处理辅助工具集
├── python-pytorch/                       # PyTorch 基础运行/推理模版
├── README.md                             # 项目整体说明文档
├── ocr.md                                # OCR 评估文档
└── 大创法藏版本20260201.docx             # 项目记录文档
```

---

## 三、 管线运行指南

运行管线需要使用服务器上的 `python39` conda 虚拟环境（该环境已预装 OpenCV, PyTorch, basicsr 等依赖）：

### 1. 一键全流程处理（单张 / 批量）
```bash
# 处理单张图像
/root/miniconda3/envs/python39/bin/python /mnt/ruanxh/7891/scripts/pipeline.py \
    --input /path/to/input.png --output /path/to/output.png --step all

# 批量处理目录
/root/miniconda3/envs/python39/bin/python /mnt/ruanxh/7891/scripts/pipeline.py \
    --input /path/to/input_dir --output /path/to/output_dir --step all
```

### 2. 模块化分步运行 (与网页前端的 4 个模块完全对应)
您可以通过 `--step` 指定运行单一模块，便于分步调优：

```bash
# 步骤 1: 仅运行去网格模型
/root/miniconda3/envs/python39/bin/python /mnt/ruanxh/7891/scripts/pipeline.py \
    --input /path/to/input.png --output /path/to/step1_out.png --step 1

# 步骤 2: 仅运行 Restormer 去模糊
/root/miniconda3/envs/python39/bin/python /mnt/ruanxh/7891/scripts/pipeline.py \
    --input /path/to/step1_out.png --output /path/to/step2_out.png --step 2

# 步骤 3: 仅运行 ResShift 超分 4 倍放大
/root/miniconda3/envs/python39/bin/python /mnt/ruanxh/7891/scripts/pipeline.py \
    --input /path/to/step2_out.png --output /path/to/step3_out.png --step 3

# 步骤 4: 仅运行 Reinhard 色彩校正 (必须通过 --ref_input 指定原图对齐色调)
/root/miniconda3/envs/python39/bin/python /mnt/ruanxh/7891/scripts/pipeline.py \
    --input /path/to/step3_out.png --output /path/to/final_out.png --step 4 \
    --ref_input /path/to/input.png
```

---

## 四、 高清化管线参数调节指南

### 1. Frequency Descreening (去网格) 参数调节

| 参数名称 | 命令行标记 | 取值范围 | 默认值 | 调整建议与说明 |
| :--- | :--- | :--- | :--- | :--- |
| **跳过此步** | `--no_descreen` | `False / True` | *False (默认运行)* | 如果图像无印刷网点、条纹或扫描噪声，建议开启此标记跳过。 |
| **检测幅值阈值** | `--descreen_T` | `0.1 - 10.0` | `2.0` | 频域异常噪点检测的阈值系数。数值越小，滤除越强；但过小可能会将字迹本身的边缘信号滤除。 |
| **直流分量保护半径** | `--descreen_R` | `1 - 128` | `20` | 中心低频主图像信号的保护半径。半径内的中心低频区不进行陷波滤波，防止图像严重糊化。 |
| **滑窗分块大小** | `--descreen_patch_size` | `1 - 1024` | `256` | 去网格处理的内部滑窗方块尺寸。 |
| **滑窗步长** | `--descreen_stride` | `1 - 1024` | `128` | 分块步长，通常设为分块大小的一半以重合过渡。 |

### 2. Restormer (去模糊) 参数调节

| 参数名称 | 命令行标记 | 取值范围 | 默认值 | 调整建议与说明 |
| :--- | :--- | :--- | :--- | :--- |
| **模型权重** | `--restormer_weights` | `字符串 (文件路径)` | `/mnt/ruanxh/7891/experiments/models/restormer_motion.pth` | 可以更改为官方去模糊权重 `/mnt/ruanxh/Restormer/Motion_Deblurring/pretrained_models/motion_deblurring.pth` 进行效果对比。 |
| **推理分块大小** | `--tile` | `None / 128 - 1024` | `None` (不分块直接处理) | 对于超大图像（如大于 2000x2000 像素），直接推理可能会导致显存溢出（OOM）。建议设置为 `512` 或 `256` 以减少显存占用。 |
| **重叠像素数** | `--tile_overlap` | `16 - 128` | `32` (像素值) | 相邻分块之间的重叠边缘，用于消除块拼接时的接缝纹。通常保持默认即可。 |

### 3. ResShift (超分辨率) 参数调节

| 参数名称 | 命令行标记 | 取值范围 | 默认值 | 调整建议与说明 |
| :--- | :--- | :--- | :--- | :--- |
| **模型版本** | `--version` | `v3 / v1` | `v3` | 1. **`v3` (推荐)**：残差移位第3代模型，仅需 4 步采样迭代，速度极快，伪影少。<br>2. **`v1`**：第1代模型，需要 15 步采样迭代（会加载 `resshift_realsrx4_s15_v1.pth` 权重）。 |
| **超分任务模式** | `--task` | `realsr / bicubic` | `realsr` | 1. **`realsr` (推荐)**：针对真实场景复杂退化（发黄、霉斑、纸张渗透、脱色模糊）。<br>2. **`bicubic`**：针对经典双三次下采样退化。 |
| **超分分块大小** | `--chop_size` | `None / 128 - 1024` | `None` (不分块直接超分) | 如果在超分过程中 GPU 显存报错，建议将其设置为 `256` 进行滑窗式超分辨率重建。 |
| **放大倍率** | `--scale` | `4` | `4` | 设定超分放大的物理倍数，默认 4 倍。 |

### 4. Reinhard (色彩校正) 机制

* Reinhard 色彩校正是基于统计学对齐的全自动算法，负责在 Lab 空间中将超分后图像的色彩分布（均值与方差）投射并对齐回原始原图。
* **无需参数调节**。它能极其稳定地锁定原图纸张底色，杜绝了扩散模型容易导致的字迹泛白和色泽失真。

---

## 五、 cpolar 内网穿透服务映射

为了实现从外网环境访问远程服务器 `ruanxh-bbd5` 上的各项服务，已使用 cpolar 配置并挂起了以下公网穿透映射：

| 通道名称 | 协议类型 | 容器内部端口 | 外部公网访问 URL |
| :--- | :--- | :--- | :--- |
| **ssh** | tcp | `22` (SSH 服务) | **`tcp://3.tcp.cpolar.cn:12185`** |
| **website** | http | `8080` (网站运行) | **`http://beec141c.cpolar.cn`** |
| **frontend** | http | `5175` (前端开发服务) | **`http://73703511.cpolar.cn`** |
| **backend** | http | `8000` (后端 API 服务) | **`http://81cfd8c7.cpolar.cn`** |

